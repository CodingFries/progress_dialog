name: progress_dialog_null_safe
description: Reviving an old progress dialog
version: 3.0.0
homepage: https://github.com/CodingFries/progress_dialog

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  assets:
    - assets/